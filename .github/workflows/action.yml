# action.yml
name: 'Build Release'
inputs: 
  project_dir: 
    description: 'The directory of the project'
    required: true
outputs:
  bin_file: # id of output
    description: 'The generated binary file'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.project_dir }}
