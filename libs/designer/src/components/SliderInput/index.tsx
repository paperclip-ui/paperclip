import React from "react";
import * as styles from "@paperclip-ui/designer/src/styles/slider-input.pc";

export const SliderInput = () => {
  const dotStyle = { left: `10%` };
  return <styles.Slider dotStyle={dotStyle}></styles.Slider>;
};
