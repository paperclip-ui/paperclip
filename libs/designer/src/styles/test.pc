import "./input.pc" as imp
import "./theme.pc" as theme

/**
 * @bounds(x: -507, y: -1, width: 584, height: 442)
*/

public component A {
    render div {
        style {
            position: relative
            background: white
            font-family: var(theme.defaultFontFamily)
            font-size: var(theme.fontSize05)
        }
        slot children {
            text "something else"
        }
    }
}

/**
 * @bounds(x: 249, y: 9, width: 615, height: 418)
*/

A

