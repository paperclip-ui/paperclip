import "./input.pc" as imp
import "./editor.pc" as mod1
import "./theme.pc" as theme

public style something

/**
 * @bounds(x: 532, y: 309, width: 472, height: 178)
*/

public component A {
    render div {
        style {
            position: relative
            background: white
            font-family: var(theme.defaultFontFamily)
        }
        slot children {
            text "insert something into me"
        }
    }
}

/**
 * @bounds(x: 1112, y: 295, width: 337, height: 224)
*/

public component B {
    render A {
        insert children {
            text "something"
            div {
                style {
                    font-family: var(theme.defaultFontFamily)
                    color: var(theme.blue02)
                }
                slot children {
                    text "another slot"
                }
            }
        }
    }
}

/**
 * @bounds(x: 1518, y: 275, width: 489, height: 326)
*/

B {
    insert children {
        text "a child"
    }
}

/**
 * @bounds(x: 1164, y: 698, width: 1024, height: 768)
*/

B {
    insert children {
        text "something else"
    }
}

