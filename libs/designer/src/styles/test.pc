import "./editor.pc" as mod1
import "./theme.pc" as mod2

style test {
    position: fixed
}

/**
 * @bounds(x: 0, y: 0, width: 1423, height: 1184)
*/

div {
    style extends test {
        background: var(mod2.background01)
        height: 100vh
        width: 100vw
        color: var(mod2.purple01)
        font-size: var(mod2.fontSize04)
    }
    div {
        style {
            background: var(mod2.backgroundSelectedText)
            left: 50%
            top: 50%
            width: 50%
            height: 50vh
            transform: translate(-50%, -50%)
            position: absolute
        }
    }
}

