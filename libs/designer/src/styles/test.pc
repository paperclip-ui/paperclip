import "./input.pc" as imp
import "./editor.pc" as mod1
import "./theme.pc" as theme

token test2 red
style baab {
    color: blue
    font-family: var(theme.defaultFontFamily)
}

style abba {
    font-size: 32px
}

/**
 * @bounds(x: 631, y: 1, width: 168, height: 75)
*/

public component todoItem {
    variant checked
    render div todoItem {
        style {
            display: flex
            align-items: center
            gap: var(theme.space05)
        }
        style variant checked {
            text-decoration: line-through
        }
        style variant other {
            color: red
        }
        style variant checked + other {
            font-size: 32px
        }
        div checkbox {
            style variant checked {
                background: var(theme.blue02)
                height: 1em
                width: 1em
            }
            style {
                background: rgba(0, 0, 0, 0.1)
                height: 1em
                width: 1em
                border-radius: var(theme.space02)
                position: relative
            }
            text "✓" {
                style variant checked {
                    display: block
                    font-size: 0.8em
                }
                style {
                    color: white
                    position: absolute
                    left: 50%
                    top: 50%
                    transform: translate(-50%, -50%)
                    font-size: 32px
                    display: none
                }
            }
        }
        slot children {
            text "Item"
        }
    }
}

/**
 * @bounds(x: 0, y: 0, width: 612, height: 553)
*/

div {
    style {
        position: relative
        background: white
        width: 100vw
        height: 100vh
        font-family: var(theme.defaultFontFamily)
    }
    div container {
        style {
            max-width: 400px
            margin: 0px auto
            height: 553px
            left: NaNpx
            top: NaNpx
            position: relative
            width: 400px
            display: flex
            flex-direction: column
            gap: var(theme.space05)
            padding: var(theme.space07) 0px
        }
        div header {
            style {
                font-size: var(theme.fontSize06)
            }
            text "Todos"
        }
        div inputs {
            style {
                display: flex
                gap: var(theme.space05)
            }
            input text {
                style {
                    background: var(theme.gray0)
                    width: 100%
                    border-radius: var(theme.space02)
                    border: none
                    padding: var(theme.space03)
                }
            }
            button submit {
                style {
                    background: var(theme.gray06)
                    color: var(theme.gray0)
                    border: none
                    border-radius: var(theme.space02)
                    padding: var(theme.space03) var(theme.space05)
                }
                text "add"
            }
        }
        div items {
            style {
                display: flex
                gap: var(theme.space05)
                flex-direction: column
            }
            todoItem {
                insert children {
                    text "eggs"
                }
            }
            todoItem {
                insert children {
                    text "bacon"
                }
            }
            todoItem {
                insert children {
                    text "almond milk"
                }
            }
            todoItem {
                insert children {
                    text "hard cheese"
                }
            }
        }
    }
}

/**
 * @bounds(x: 434, y: 640, width: 349, height: 181)
*/

public component Test {
    variant blarg
    variant test
    render div testRoot {
        style {
            position: relative
            background: white
            font-family: var(theme.defaultFontFamily)
            padding: var(theme.space05)
        }
        slot children {
            text "Something" {
                style {
                    color: blue
                }
            }
        }
    }
}

/**
 * @bounds(x: 813, y: 636, width: 299, height: 193)
*/

public component Test2 {
    render Test test {
        slot children {
            text "try inserting child into me" {
                style {
                    color: orange
                }
            }
        }
    }
}

/**
 * @bounds(x: 813, y: 899, width: 299, height: 193)
*/

Test2 test2Instance

/**
 * @bounds(x: 437, y: 880, width: 281, height: 198)
*/

Test testInstance

/**
 * @bounds(x: 129, y: 1341, width: 523, height: 282)
*/

div {
    style {
        position: relative
        color: blue
        background: white
    }
    text "something like this" {
        style {
            font-family: Helvetica
            position: relative
            width: 132px
            height: 19px
        }
    }
}

