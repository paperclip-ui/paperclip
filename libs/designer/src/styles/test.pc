import "./input.pc" as imp
import "./theme.pc" as theme

/**
 * @bounds(x: 576, y: 218, width: 483, height: 217)
*/

public component Something {
    variant other trigger {
        ".other"
    }
    render div {
        style variant other {
            background: var(theme.blue02)
        }
        style {
            position: relative
            font-family: var(theme.defaultFontFamily)
            color: var(theme.gray0)
            background: var(theme.gray05)
        }
        text "something" {
            style variant other {
                color: var(theme.blue00)
            }
        }
    }
}

/**
 * @bounds(x: 1092, y: 222, width: 315, height: 191)
*/

Something(class: other)

