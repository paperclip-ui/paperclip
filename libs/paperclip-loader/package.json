{
  "name": "paperclip-loader",
  "version": "0.1.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "build:watch": "concurrently \"yarn build:tsc --watch\" \"yarn build:rs --watch\"",
    "build:tsc": "tsc",
    "build:rs": "cargo-cp-artifact -nc lib/index.node -- cargo build --message-format=json-render-diagnostics",
    "build:rs:debug": "yarn build --",
    "build:rs:release": "yarn build -- --release",
    "install": "yarn build-release",
    "test": "cargo test"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cargo-cp-artifact": "^0.1"
  }
}
