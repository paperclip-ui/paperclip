// use super::core::{FileReader, FileResolver};

// pub struct EmbeddableFileResolverOptions {
//   embed_max_size: u64
// }

// pub struct EmbeddableFileResolver<TReader: FileReader, TResolver: FileResolver> {
//   options:
//   reader: TReader,
//   resolver: TResolver
// }

// impl<TReader: FileReader, TResolver: FileResolver> FileResolver for EmbeddableFileResolver<TReader, FileResolver> {

// }
