import "../../../../atoms/view.pc" as imp0
import "./layer.pc" as imp1


/**
 * @bounds(left: 376, top: 199, right: 648, bottom: 452)
 */
public component OpenModule {
  script (src: "./open-module-controller.tsx")
  render div {
    style extends imp0.baseFont {
      box-sizing: border-box
      flex-direction: column
      display: inline-block
      min-width: 100%
    }
    div content {
      style {
        box-sizing: border-box
        display: block
      }
      imp1.BaseLayer instance122(label1: "Module") {
        style {
          box-sizing: border-box
          display: flex
          padding-left: 16px
          padding-top: 8px
          padding-right: 16px
          padding-bottom: 8px
          align-items: center
          justify-content: space-between
        }
        override {
          variant header (enabled: [true])
          variant pcLayer (enabled: [true])
          variant file (enabled: [true])
        }
        override circleIcon {
          style {
          }
        }
      }
      imp1.BaseLayer element1234 (label1: "Button") {
        style {
          box-sizing: border-box
          display: flex
          padding-left: 34px
          padding-top: 8px
          padding-right: 16px
          padding-bottom: 8px
          align-items: center
          justify-content: space-between
        }
        override {
          variant component2 (enabled: [true])
          variant expanded (enabled: [true])
          variant component2 (enabled: [true])
        }
        override circleIcon {
          style {
          }
        }
      }
      imp1.BaseLayer instance12(label1: "Hello World") {
        style {
          padding-left: 50px
        }
        override {
          variant alt (enabled: [true])
          variant alt (enabled: [true])
        }
        override textIcon {
          style {
            margin-left: 2px
            top: -8px
          }
        }
      }
      imp1.BaseLayer element122(label1: "div") {
        style {
          padding-left: 50px
        }
        override {
          variant element (enabled: [true])
          variant selected (enabled: [true])
          variant element (enabled: [true])
        }
        override textIcon {
          style {
            margin-left: 2px
            top: -8px
          }
        }
      }
      imp1.BaseLayer element12(label1: "Button") {
        style {
          box-sizing: border-box
          display: flex
          padding-left: 34px
          padding-top: 8px
          padding-right: 16px
          padding-bottom: 8px
          align-items: center
          justify-content: space-between
        }
        override {
          variant instance (enabled: [true])
          variant expanded (enabled: [true])
          variant instance (enabled: [true])
          variant alt (enabled: [true])
        }
      }
      imp1.BaseLayer inst123(label1: "Shadow") {
        style {
          padding-left: 50px
        }
        override {
          variant shadow (enabled: [true])
          variant alt (enabled: [true])
          variant shadow (enabled: [true])
        }
      }
      imp1.BaseLayer inst144(label1: "Text") {
        style {
          padding-left: 50px
        }
        override {
          variant alt (enabled: [true])
          variant plug (enabled: [true])
        }
      }
    }
  }
}

